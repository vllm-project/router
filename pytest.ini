[pytest]
testpaths = py_test
python_files = test_*.py
python_classes = Test*
python_functions = test_*

[tool:pytest]
markers =
    integration: marks tests as integration tests
    manual: marks tests as manual tests (requiring system access)
