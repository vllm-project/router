[package]
name = "wasm_middleware_example"
version = "0.1.0"
edition = "2021"
publish = false
description = "Example OnRequest WASM middleware for vLLM Router"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.46"
